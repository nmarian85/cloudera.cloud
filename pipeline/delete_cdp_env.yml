
delete_cdp_env:
  stage: delete_cdp_env
  script:
    - cdp --debug environments delete-environment --environment-name $CDP_ENV_NAME || true
    - timeout 90m bash scripts/wait_for_del.sh "ENV"
  rules:
    - if: $CI_PIPELINE_SOURCE != "schedule" && $ACTION == "delete"
      when: manual
