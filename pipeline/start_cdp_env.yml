---
cdp_env_start:
  stage: start
  script:
    - cdp environments start-environment --environment-name $ENV_NAME || true
    - timeout 20m bash scripts/wait_for_status.sh "AVAILABLE"
  rules:
    - if: $ACTION == "start"
