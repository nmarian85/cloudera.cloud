---
.before_provision: &before_provision
  - cdp environments start-environment --environment-name $ENV_NAME || true
  - timeout 20m bash cdp-env/start_env.sh

cml_provision:
  stage: cml_provision
  before_script:
    - *before_provision
    - cdp ml create-workspace --generate-cli-skeleton > skel.json
    - python3 cml/cml_json_provision.py
    - cat cml_*.json
  script:
    - ls -1 cml_*.json | xargs -I '{}' cdp --debug ml create-workspace --cli-input-json file://'{}'
  artifacts:
    paths:
      - skel.json
      - cml_*.json
    expire_in: 30 days
    when: always
  rules:
    - if: $CI_PIPELINE_SOURCE != "schedule"
      when: manual
